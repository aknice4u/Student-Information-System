@model IEnumerable<Domain.FiscalYear>

@{
    ViewBag.Title = "ViewFiscalYear";
}

<h2>ViewFiscalYear</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FiscalYear_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FiscalYear_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FiscalYear_ID })
        </td>
    </tr>
}

</table>
